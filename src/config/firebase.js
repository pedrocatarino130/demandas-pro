/**
 * Configuração Firebase - REMOVIDO
 * Este arquivo mantém apenas stubs vazios para compatibilidade.
 * O sistema agora funciona apenas com armazenamento local.
 */

// Exportar objetos vazios para compatibilidade
export const app = null;
export const db = null;
export const auth = null;

export default {
    app: null,
    db: null,
    auth: null
};

